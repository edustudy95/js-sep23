let name = "Rajan"   // {its datatype is STRING}

let age = 18     // {its datatype is NUMBER}

let isLoggedIn = false   // {its datatype is BOOLEAN}

let state;          // {its datatype is UNDEFINED}

// Premitive Data type
    // number => 2 to the power 53
    // bigint
    // string => ""
    // boolean => true/false
    // null => its standalone value     {its datatype is OBJECT}
    // undefined => value not assigned  {its datatype is UNDEFINED}
    // symbol => unique


// Object Data type

console.log(typeof null);       // {its datatype is OBJECT}
console.log(typeof undefined);  // {its datatype is UNDEFINED}
console.log(typeof state);